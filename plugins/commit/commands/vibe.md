---
description: Audit the current diff and verification evidence without editing files.
argument-hint: '[--full]'
disable-model-invocation: true
---

Use $commitment-issues to audit the raw `$ARGUMENTS` unchanged. Review the focused diff and focused checks by default; with `--full`, run the repository's broader documented suite. Make no edits and perform no public action.
